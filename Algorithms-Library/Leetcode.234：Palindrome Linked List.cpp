#include"LinkList.h"

bool PalindromeLinkedList::isPalindrome(ListNode* head) {
	ListNode* temp = head;
	int length = 0;

	while (temp)
	{
		length++;
		temp = temp->next;
	}

	for (int i = 0; i < length / 2; i++)
	{
		if ()
		{

		}
	}
}